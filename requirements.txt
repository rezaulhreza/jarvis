# Core dependencies
ollama>=0.3.0
rich>=13.0.0
pyyaml>=6.0

# Token counting
tiktoken>=0.5.0

# Web search
duckduckgo-search>=4.0.0

# HTTP client (for ElevenLabs TTS)
httpx>=0.27.0

# Text-to-speech
edge-tts>=7.0.0

# PDF processing (for RAG)
pypdf>=4.0.0

# Vector database (for RAG)
qdrant-client>=1.9.0  # Default: Qdrant cloud (when QDRANT_URL + QDRANT_API_KEY set)
chromadb>=0.4.0  # Fallback: local vector store

# Future additions (uncomment when needed)
# python-telegram-bot>=20.0
# openai-whisper
# pyttsx3
